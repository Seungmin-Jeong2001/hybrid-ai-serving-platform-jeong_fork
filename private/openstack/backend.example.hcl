# Example backend config for remote Terraform state.
# Provide real values through GitHub Actions secrets or environment-specific files.
bucket = "replace-with-state-bucket"
key    = "private-cloud-foundation/openstack.tfstate"
region = "replace-with-region"
