apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: grafana-sre-dashboards
    files:
      - dashboards/service-dashboard.json
      - dashboards/infra-dashboard.json
    options:
      disableNameSuffixHash: true
