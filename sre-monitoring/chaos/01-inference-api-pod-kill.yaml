apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: inference-api-pod-kill
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - inference
    labelSelectors:
      app: inference-api
  duration: "30s"
