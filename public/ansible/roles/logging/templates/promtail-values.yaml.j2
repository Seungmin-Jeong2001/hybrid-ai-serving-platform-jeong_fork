config:
  clients:
    - url: http://loki-gateway.{{ logging_namespace }}.svc.cluster.local/loki/api/v1/push

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"

serviceMonitor:
  enabled: true
