apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: inference-api
  namespace: inference
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: inference-api
  podMetricsEndpoints:
    - port: http
      path: /metrics
      interval: 30s
