# worker나 api가 HTTP 호출해야 함

apiVersion: v1
kind: Service
metadata:
  name: kserve-predictor
  namespace: hasp
spec:
  selector:
    app: kserve-predictor
  ports:
    - port: 80
      targetPort: 8080
